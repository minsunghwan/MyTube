extends base

block content
    if errorMessage
        span=errorMessage
    form(method="POST" type="multipart/form-data")
        label(for="video") 
        input(type="file" accept="video/*" id="video" name="video" required)
        label(for="thumb") 
        input(type="file" accept="image/*" id="thumb" name="thumb" required)
        input(placeholder="Title" type="text" id="title" name="title" maxlength=30 required)
        input(placeholder="Description" type="description" id="description" name="title" minlength=5 required)
        input(placeholder="Hashtags , separated by comma."  type="text" name="hashtag" required)
        input(type="submit" value="Upload Video")


