mixin video(video)
    div.video-mixin
        img.video-mixin__thumb(src=video.thumbUrl ,alt=video.title )
        div.video-mixin__data
            a(href=`/videos/${video.id}`)=video.title
            div.video-mixin__meta
                span.video-mixin__meta-owner #{video.owner.name}
                span.video-mixin__meta-views 조회수 #{video.meta.views} 회
            